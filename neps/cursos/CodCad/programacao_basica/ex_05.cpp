#include <bits/stdc++.h>

using namespace std;

int main() {

    // Bondinho
    int a, m, total;
    cin >> a >> m;
    total = a + m;

    if (total <= 50) cout << "S" << endl;
    else cout << "N" << endl;
}